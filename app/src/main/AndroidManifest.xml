<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dietary">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Activites.EditProtegeWorkoutActivity" />
        <activity android:name="Activites.EditProtegeDietActivity" />
        <activity android:name="Activites.ExercisesInfoActivity" />
        <activity android:name="Activites.InformationActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDzdWP0J9qe4I6TvdBQao0bKPC25k7m_O0" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="Activites.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name="Activites.ExerShow" />
        <activity android:name="Activites.DietShow" />
        <activity android:name="Activites.AnotherExerActivity" />
        <activity android:name="Activites.AnotherUserDiet" />
        <activity android:name="Activites.AnotherUserProfile" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9436822335656645~5861390755" />

        <activity android:name="Activites.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Activites.PassChangeActivity" />
        <activity android:name="Activites.AddPersonActivity" />
        <activity android:name="Activites.CoachesList" />
        <activity
            android:name="Activites.ExerciseActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="Activites.AddDietActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name="Activites.ProtegesList" />
        <activity
            android:name="Activites.CreateAccountActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="Activites.LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".NavigationBar"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>